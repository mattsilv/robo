import Foundation

/// Single source of truth for user-facing strings.
enum AppStrings {
    enum Tabs {
        static let inbox = "Inbox"
        static let gather = "Gather"
        static let history = "History"
        static let settings = "Settings"
    }
}
